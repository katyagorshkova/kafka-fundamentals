1. Create two topics:

docker exec -ti kafka /usr/bin/kafka-topics --create --bootstrap-server kafka:19092 --topic Numbers --partitions 10
 
docker exec -ti kafka /usr/bin/kafka-topics --create --bootstrap-server kafka:19092 --topic CountedNumbers --partitions 10
 
 
2. Run examples.streams.CountNumbersExample

  The application must be running
  
3. Run examples.streams.CountNumbersExampleDriver


4. Check the topic CountedNumbers
