1. Create a table and produce some data

CREATE TABLE simple_table (
  id int PRIMARY KEY,
  value VARCHAR ( 255 ) NOT NULL
  );
  
  insert into simple_table values(1,'a');
  insert into simple_table values(2,'b');
  insert into simple_table values(3,'c');
  insert into simple_table values(4,'d');
  
2. Check connector plugins 

curl http://localhost:8083/connector-plugins

3. Check connectors

curl  http://localhost:8083/connectors

4. Create a connector 

curl -X POST -H 'Content-Type: application/json' -d @postgres-source-connector.json http://localhost:8083/connectors

5. Check the status

curl http://localhost:8083/connectors/postgres-source-connector/status

6. Check the messages

docker exec -ti kafka1 /usr/bin/kafka-console-consumer --bootstrap-server kafka1:19092 --topic connect-jdbc-simple_table --from-beginning --group lab1

7. Add more data

  insert into simple_table values(5,'e');
  insert into simple_table values(6,'f');
  
8. Check the messages